package com.integrador.spring.app.Modelo;

//Define los roles diponibles para los usuarios del sistema
public enum role {
    ADMIN, //ROl de administrador con permisos elevados
    USER //Rol de usuario con permisos limitados 
}
